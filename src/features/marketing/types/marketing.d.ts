export type NodeProps = {
  heading: string;
  text: string;
  borderColor?: string;
  dotColor?: string;
  glow?: string;
  dotClasses?: string;
  nodeClasses?: string;
}

export type RuntimeCardProps = {
  heading: string;
  text: string;
  textColor?: string;
}